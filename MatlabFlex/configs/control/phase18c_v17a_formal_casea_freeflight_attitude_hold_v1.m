function [cfg,measurement,profile] = ...
        phase18c_v17a_formal_casea_freeflight_attitude_hold_v1(cfg,source)
%PHASE18C_V17A_FORMAL_CASEA_FREEFLIGHT_ATTITUDE_HOLD_V1 Formal Case-A profile.
%   Configures the approved default-inactive, symmetric longitudinal,
%   free-flight Case-A audit at the exact V17A [40 m/s, 1 deg] source.

arguments
    cfg struct
    source struct
end

required = ["beam","base","trim","p5","sourceId", ...
    "registryPath","packagePath","certificatePath", ...
    "packageSha256","certificateSha256"];
for field = required
    assert(isfield(source,field),"AeroFlex:FormalCaseAProfileSource", ...
        "The formal Case-A source is missing %s.",field);
end
expectedRegistryHash = ...
    "c3dcb391b61caf163bcc6781e239be94992e9ef651a56040c17a2aec86c62c2c";
assert(isfile(source.registryPath) && ...
    localFileHash(source.registryPath)==expectedRegistryHash, ...
    "AeroFlex:FormalCaseAProfileRegistry", ...
    "The V17A registry is unavailable or changed.");
assert(isfile(source.packagePath) && ...
    localFileHash(source.packagePath)==string(source.packageSha256), ...
    "AeroFlex:FormalCaseAProfilePackage", ...
    "The V17A Case-A package hash changed.");
assert(isfile(source.certificatePath) && ...
    localFileHash(source.certificatePath)==string(source.certificateSha256), ...
    "AeroFlex:FormalCaseAProfileCertificate", ...
    "The V17A Case-A certificate hash changed.");
assert(string(source.sourceId)=="CASEA_U040_ALPHA_P01" && ...
    norm(double(source.p5.mu(:).')-[40,1],inf)<=1e-12, ...
    "AeroFlex:FormalCaseAProfileSource", ...
    "Formal Case A requires the exact [40 m/s, 1 deg] V17A source.");
assert(isfinite(source.trim.thrust) && source.trim.thrust>=0, ...
    "AeroFlex:NegativeTrimThrust", ...
    "Formal Case A requires nonnegative trim thrust.");

bodyCase = "";
if isfield(cfg,"sim") && isfield(cfg.sim,"bodyCase")
    bodyCase = lower(erase(string(cfg.sim.bodyCase),"_"));
elseif isfield(cfg,"sim") && isfield(cfg.sim,"body_case")
    bodyCase = lower(erase(string(cfg.sim.body_case),"_"));
end
assert(bodyCase=="coupledfull", "AeroFlex:FormalCaseAProfileBodyCase", ...
    "Formal Case A requires symmetric coupled-full free flight.");
assert(abs(cfg.ctrl.Ts-0.01)<=1e-14 && ...
    abs(cfg.sim.dt-source.p5.parConst.dt)<=1e-14, ...
    "AeroFlex:FormalCaseAProfileTime", ...
    "Formal Case A requires the source plant step and a 0.01 s control sample.");

massOwner = struct("owner","legacy_checkpoint_not_overridden", ...
    "sourceWingOwned",false,"candidateActive",false);
if isfield(cfg,"rigidEOMset") && isstruct(cfg.rigidEOMset) && ...
        isfield(cfg.rigidEOMset,"massOwnership") && ...
        string(cfg.rigidEOMset.massOwnership.owner)== ...
        "nonwing_source_owned_wing_reaction"
    [cfg,massOwner] = RigidBody.methods.applyRuntimeMassOwner(cfg, ...
        "nonwing_source_owned_wing_reaction");
    massOwner.candidateActive = true;
    cfg.trim.shiftWingMomentToCG = true;
    cfg.sim.shiftWingMomentToCG = true;
end

cfg.ctrl.mheSolver = "custom_sqp";
cfg.ctrl.mheSqp.HessianMode = "gauss_newton";
cfg.ctrl.mheSqp.MaxIterations = 20;
cfg.ctrl.mheSqp.LineSearchStrategy = "safeguarded_hybrid";
cfg.ctrl.mheSqp.UseValueOnlyMeritEvaluation = true;
cfg.ctrl.mpcSolver = "custom_sqp";
cfg.ctrl.sqp.HessianMode = "gauss_newton";
cfg.ctrl.sqp.MaxIterations = 5;
cfg.ctrl.sqp.LineSearchStrategy = "l1_merit";
cfg.ctrl.sqp.LineSearchUpdate = "fixed_beta";
cfg.ctrl.sqp.UseValueOnlyMeritEvaluation = true;
cfg.ctrl.sqp.SecondOrderCorrectionEnabled = false;
cfg.ctrl.caseAEndpointRealization = struct( ...
    "enabled",true,"normalizeEndpointRateEquality",true, ...
    "fallback","preceding_accepted_endpoint");
cfg.caseAcceptance.minimumSolveSuccessRate = 0.975;
cfg.sim.commandsAreTrimIncrements = true;
cfg.fusion.enable = true;
cfg.fusion.omega_c = 2.5;
cfg.fusion.innerIsDeviation = false;
cfg.actuator.enable = true;
cfg.actuator.omega = 40;
cfg.actuator.deltaL = cfg.uL;
cfg.actuator.deltaU = cfg.uU;
cfg.actuator.rateL = cfg.urateL;
cfg.actuator.rateU = cfg.urateU;

intrinsicPortEnabled = isfield(source, ...
    "intrinsicPortRuntimeIntegration") && ...
    isstruct(source.intrinsicPortRuntimeIntegration) && ...
    isfield(source.intrinsicPortRuntimeIntegration,"enabled") && ...
    logical(source.intrinsicPortRuntimeIntegration.enabled);
if intrinsicPortEnabled
    portInfo = source.intrinsicPortRuntimeIntegration;
    assert(logical(portInfo.auditOnly) && ...
        string(portInfo.sourceId)=="CASEA_U040_ALPHA_P01" && ...
        isfield(portInfo,"runtimeRateProjectionEnabled") && ...
        logical(portInfo.runtimeRateProjectionEnabled) && ...
        isfield(source.p5.p5,"intrinsicPortRuntimeIntegration") && ...
        logical(source.p5.p5.intrinsicPortRuntimeIntegration.enabled), ...
        "AeroFlex:FormalCaseAIntrinsicPortContract", ...
        "The formal Case-A intrinsic-port candidate is incomplete.");
    cfg.ctrl.fixedBasisIntrinsicPortRuntimeIntegration = struct( ...
        "enabled",true,"auditOnly",true,"defaultActive",false, ...
        "sourceId","CASEA_U040_ALPHA_P01", ...
        "candidatePackageHash",string(portInfo.candidatePackageHash), ...
        "runtimeRateProjectionEnabled",true, ...
        "fallback","accepted_registry_bound_casea_package");
else
    cfg.ctrl.fixedBasisIntrinsicPortRuntimeIntegration = struct( ...
        "enabled",false,"auditOnly",true,"defaultActive",false, ...
        "sourceId","CASEA_U040_ALPHA_P01", ...
        "candidatePackageHash","", ...
        "runtimeRateProjectionEnabled",false, ...
        "fallback","accepted_registry_bound_casea_package");
end

repositoryRoot = fileparts(fileparts(fileparts(fileparts(mfilename("fullpath")))));
gainManifestPath = fullfile(repositoryRoot,"context","audits", ...
    "phase18c-control-validation","lqr", ...
    "v17a-casea-runtime-centered-source-bank-v1", ...
    "phase18c_v17a_casea_runtime_centered_longitudinal_lqr_gain_bank_v1.json");
gainManifestHash = ...
    "24ac30dd3d54b5c89324688e2f0991261d100980ea2b9439adb2d0d132b52ec0";
assert(isfile(gainManifestPath) && ...
    localFileHash(gainManifestPath)==gainManifestHash, ...
    "AeroFlex:FormalCaseAProfileLqrManifest", ...
    "The formal Case-A exact V17A LQR manifest is unavailable or changed.");
cfg.outerLQR.gainBank = struct("enable",true,"auditOnly",true, ...
    "sourceId","CASEA_U040_ALPHA_P01", ...
    "manifestPath",gainManifestPath, ...
    "expectedManifestSha256",char(gainManifestHash));
if isfield(source,'correctedNonwingOwner') && ...
        isstruct(source.correctedNonwingOwner) && ...
        isfield(source.correctedNonwingOwner,'enabled') && ...
        logical(source.correctedNonwingOwner.enabled)
    cfg.outerLQR.correctedNonwingOwner = source.correctedNonwingOwner;
end
if ~isfield(cfg.outerLQR,'sourceCoordinateDynamicOutputFeedback')
    cfg.outerLQR.sourceCoordinateDynamicOutputFeedback = struct( ...
        'enable',false,'defaultActive',false,'exactCaseAOnly',true);
end

packageOutputEnabled = false;
if isfield(cfg.ctrl,"packageOwnedPhysicalOutputs") && ...
        isstruct(cfg.ctrl.packageOwnedPhysicalOutputs) && ...
        isfield(cfg.ctrl.packageOwnedPhysicalOutputs,"enabled")
    packageOutputEnabled = logical( ...
        cfg.ctrl.packageOwnedPhysicalOutputs.enabled);
end
assert(isscalar(packageOutputEnabled), ...
    "AeroFlex:FormalCaseAProfileOutputPolicy", ...
    "The package-owned physical-output switch must be scalar.");
measurementSource = source;
outputPolicy = "legacy_casea_closed_checkpoint";
cfg.ctrl.nmpcWingtipOutput = struct("enabled",true, ...
    "stageWeight",1e4,"terminalWeight",0, ...
    "ownerPolicy",outputPolicy, ...
    "profile","phase18c_v17a_formal_casea_freeflight_attitude_hold_v1");
if packageOutputEnabled
    request = cfg.ctrl.packageOwnedPhysicalOutputs;
    assert(isfield(request,"policy") && ...
        string(request.policy)=="package_owned_signed_mirrored_tip" && ...
        isfield(source,"packageOwnedScheduleStateCoordinate"), ...
        "AeroFlex:FormalCaseAProfileOutputPolicy", ...
        ['The corrected formal Case-A output path requires the qualified ', ...
         'package-owned policy and active package chart.']);
    measurementSource.scheduleStateCoordinate = ...
        source.packageOwnedScheduleStateCoordinate;
    outputContract = ...
        AeroFlex.sensor.buildSignedWingtipOutput(measurementSource);
    outputPolicy = outputContract.ownerPolicy;
    cfg.ctrl.nmpcWingtipOutput.ownerPolicy = outputPolicy;
    cfg.ctrl.nmpcWingtipOutput.gradient = ...
        outputContract.symmetricGradient;
end
[cfg,measurement] = AeroFlex.sensor.buildLongitudinalPhysicalOutputAudit( ...
    cfg,measurementSource,"imu_attitude_airdata");
measurement.detail.type = ...
    "ideal symmetric-longitudinal benchmark measurement suite";
measurement.detail.benchmarkProfileEligible = true;
measurement.detail.hardwareProductionEligible = false;

profile = struct("name", ...
    "phase18c_v17a_formal_casea_freeflight_attitude_hold_v1", ...
    "formalBenchmarkCase","A", ...
    "modelRevision","V17A", ...
    "sourceId","CASEA_U040_ALPHA_P01", ...
    "sourceCoordinate",[40,1], ...
    "registrySha256",expectedRegistryHash, ...
    "measurementContract","ideal_imu_attitude_airdata", ...
    "physicalOutputOwnerPolicy",outputPolicy, ...
    "attitudeReference","profile_owned_pitch_command_or_trim_hold", ...
    "airspeedAndAltitudeReference","reported_outcomes_not_casea_references", ...
    "minimumSolveSuccessRate",0.975, ...
    "sourceCoordinateDynamicOutputFeedback", ...
        cfg.outerLQR.sourceCoordinateDynamicOutputFeedback, ...
    "rigidMassOwnership",massOwner, ...
    "wingRootMomentOwner",struct( ...
        "shiftToRigidCenterOfMass",logical( ...
            cfg.sim.shiftWingMomentToCG), ...
        "owner","rigid_mass_owner_or_legacy_profile"), ...
    "fixedBasisIntrinsicPortRuntimeIntegration", ...
        cfg.ctrl.fixedBasisIntrinsicPortRuntimeIntegration, ...
    "offlineOnly",true,"hardwareSensorClaim",false);
end

function value = localFileHash(path)
engine = javaMethod("getInstance","java.security.MessageDigest","SHA-256");
fileId = fopen(path,"rb");
assert(fileId>=0,"AeroFlex:FormalCaseAProfileHash", ...
    "Unable to read %s.",path);
cleanup = onCleanup(@() fclose(fileId));
while ~feof(fileId)
    buffer = fread(fileId,1024*1024,"*uint8");
    if isempty(buffer), break, end
    engine.update(typecast(buffer(:),"int8"));
end
value = lower(string(reshape(dec2hex(typecast(engine.digest(),"uint8"),2).',1,[])));
clear cleanup
end
